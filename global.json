{
  "//": "Pins the SDK band. The project targets net10.0-* because Play requires API 36, which .NET 9 cannot target; rollForward latestFeature lets any 10.0.x SDK build it while refusing an older major. A stale obj/project.assets.json from a different SDK is how the net9->net10 migration produced builds that 'succeeded' against the wrong TFM.",
  "sdk": {
    "version": "10.0.200",
    "rollForward": "latestFeature"
  }
}
