<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:calendar="clr-namespace:Syncfusion.Maui.Calendar;assembly=Syncfusion.Maui.Calendar"
             x:Class="Animal_Diary_App.Data.View.CalendarPage">

        <Grid>

                <!-- Background -->
                <Image Source="hintergrund_blau.png"
                       Aspect="AspectFill"
                       HorizontalOptions="Fill"
                       VerticalOptions="Fill"
                       InputTransparent="True"/>

                <!-- Main Content -->

                <VerticalStackLayout Padding="24,20"
                                     Spacing="24">

                        <!-- Calendar Card -->


                        <VerticalStackLayout Spacing="16">

                                <calendar:SfCalendar
                                        x:Name="calendar"
                                        View="Month"
                                        BackgroundColor="#FF20A5E7"
                                        SelectedDate="{Binding CalendarVM.CurrentSelectedDate, Mode=TwoWay}"/>

                                <Label Text="{Binding CalendarVM.CurrentSelectedDate, StringFormat='Selected Date: {0:d}'}"
                                       FontSize="18"
                                       FontAttributes="Bold"
                                       HorizontalOptions="Center"
                                       HorizontalTextAlignment="Center"/>

                        </VerticalStackLayout>


                        <!-- Action Buttons -->
                        <HorizontalStackLayout Spacing="14"
                                               HorizontalOptions="Center">

                                <Button Text="+ Mood"
                                        FontSize="20"
                                        WidthRequest="150"
                                        HeightRequest="50"
                                        CornerRadius="25"
                                        IsVisible="{Binding CalendarVM.IsMoodAddButtonVisible}"
                                        Command="{Binding CalendarVM.ShowMoodInputCommand}"/>

                                <Button Text="+ Weight"
                                        FontSize="20"
                                        WidthRequest="150"
                                        HeightRequest="50"
                                        CornerRadius="25"
                                        IsVisible="{Binding CalendarVM.IsWeightAddButtonVisible}"
                                        Command="{Binding CalendarVM.ShowWeightInputCommand}"/>

                                <Button Text="+ Medication"
                                        FontSize="20"
                                        WidthRequest="150"
                                        HeightRequest="50"
                                        CornerRadius="25"/>


                        </HorizontalStackLayout>

                        <!-- Mood Input -->

                        <Entry Placeholder="Enter your pet's mood for the day"
                               Text="{Binding CalendarVM.EnteredMood}"
                               FontSize="18"
                               Completed="OnMoodEntryCompleted"
                               IsVisible="{Binding CalendarVM.IsMoodInputVisible}"/>



                        <!-- Weight Input -->

                        <Entry Placeholder="Enter your pet's weight today"
                               Text="{Binding CalendarVM.EnteredWeight}"
                               FontSize="18"
                               Keyboard="Numeric"
                               Completed="OnWeightEntryCompleted"
                               IsVisible="{Binding CalendarVM.IsWeightInputVisible}"/>
                        <HorizontalStackLayout HorizontalOptions="Center">
                                <Entry Placeholder="Enter the taken medication name"
                                       FontSize="18"
                                       Keyboard="Numeric"/>
                                <Entry Placeholder="dosage"
                                       FontSize="18"
                                       Keyboard="Numeric"/>
                        </HorizontalStackLayout>

                        <!-- Current Mood Display -->


                        <Label Text="{Binding CalendarVM.ShownMood, StringFormat='Mood: {0}'}"
                               FontSize="18"
                               FontAttributes="Bold"
                               HorizontalOptions="Center"
                               HorizontalTextAlignment="Center"/>


                </VerticalStackLayout>


                <!-- Bottom Navigation -->
                <Grid RowDefinitions="*,Auto"
                      ColumnDefinitions="*,*"
                      VerticalOptions="End">

                        <Button Text="Main"
                                Grid.Row="1"
                                Grid.Column="0"
                                Clicked="OnMainClicked"/>

                        <Button Text="Calendar"
                                Grid.Row="1"
                                Grid.Column="1"/>

                </Grid>

        </Grid>

</ContentPage>