<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:helpers="clr-namespace:Animal_Diary_App.Helpers"
             xmlns:view="clr-namespace:Animal_Diary_App.Data.View"
             x:Class="Animal_Diary_App.Data.View.MainPage"
             NavigationPage.HasNavigationBar="False"
             BackgroundColor="#DCEBF5">

    <ContentPage.Resources>
        <ResourceDictionary>
            <helpers:WeightToHeightConverter x:Key="WeightToHeightConverter"/>
            <helpers:WeightMaxConverter x:Key="WeightMaxConverter"/>
            <helpers:WeightMidConverter x:Key="WeightMidConverter"/>
            <helpers:WeightMinConverter x:Key="WeightMinConverter"/>
            <helpers:InvertedBoolConverter x:Key="InvertedBoolConverter"/>
        </ResourceDictionary>
    </ContentPage.Resources>

    <Grid RowDefinitions="*,Auto">

        <!-- Background -->
        <Image Source="hintergrund_blau.png"
               Aspect="AspectFill"
               HorizontalOptions="Fill"
               VerticalOptions="Fill"
               InputTransparent="True"
               Grid.RowSpan="2"/>

        <!-- Main Content -->
        <ScrollView Grid.Row="0">

            <VerticalStackLayout Padding="20,16,20,120"
                                 Spacing="18">

                <!-- Header -->
                <Grid ColumnDefinitions="*,Auto"
                      VerticalOptions="Center">

                    <VerticalStackLayout Spacing="2">

                        <!-- ? greeting binding -->
                        <Label Text="Good morning ☀️"
                               FontSize="14"
                               FontAttributes="Bold"
                               TextColor="#4A6172"
                               Opacity="0.9"/>

                    </VerticalStackLayout>

                    <!-- ? settings command -->
                    <Border Grid.Column="1"
                            WidthRequest="38"
                            HeightRequest="38"
                            BackgroundColor="#666CDC66"
                            StrokeThickness="0"
                            HorizontalOptions="End"
                            VerticalOptions="Center">

                        <Border.StrokeShape>
                            <RoundRectangle CornerRadius="19"/>
                        </Border.StrokeShape>

                        <Image Source="settings.png"
                               WidthRequest="18"
                               HeightRequest="18"
                               HorizontalOptions="Center"
                               VerticalOptions="Center"/>
                    </Border>

                </Grid>

                <!-- ACTIVE PET HERO -->

                <Border Padding="18"
                        BackgroundColor="#5B9BB8"
                        StrokeThickness="0">

                    <Border.StrokeShape>
                        <RoundRectangle CornerRadius="28"/>
                    </Border.StrokeShape>

                    <Border.Shadow>
                        <Shadow Brush="#40000000"
                                Offset="0,8"
                                Radius="18"
                                Opacity="0.35"/>
                    </Border.Shadow>

                    <Grid ColumnDefinitions="Auto,*"
                          ColumnSpacing="14">

                        <!-- Avatar -->
                        <Grid WidthRequest="72"
                              HeightRequest="72">

                            <Border BackgroundColor="#30FFFFFF"
                                    Stroke="#55FFFFFF"
                                    StrokeThickness="2">

                                <Border.StrokeShape>
                                    <RoundRectangle CornerRadius="36"/>
                                </Border.StrokeShape>

                                <!-- ? bind pet image -->
                                <Image Source="animal1.jpg"
                                       Aspect="AspectFill"/>
                            </Border>

                            <!-- Mood / health indicator -->
                            <Border WidthRequest="16"
                                    HeightRequest="16"
                                    BackgroundColor="#6DD39E"
                                    Stroke="#5B9BB8"
                                    StrokeThickness="2"
                                    HorizontalOptions="End"
                                    VerticalOptions="End">

                                <Border.StrokeShape>
                                    <RoundRectangle CornerRadius="8"/>
                                </Border.StrokeShape>

                            </Border>

                        </Grid>

                        <!-- Pet Info -->
                        <VerticalStackLayout Grid.Column="1"
                                             Spacing="6"
                                             VerticalOptions="Center">

                            <Label Text="{Binding MainPageVM.ActivePet.Name}"
                                   FontSize="24"
                                   FontFamily="Georgia"
                                   FontAttributes="Bold"
                                   TextColor="White"/>

                            <Label FontSize="12"
                                   TextColor="#DCEBF5">

                                <Label.FormattedText>
                                    <FormattedString>
                                        <Span Text="{Binding MainPageVM.ActivePet.Type}"/>
                                        <Span Text=" · "/>
                                        <Span Text="{Binding MainPageVM.ActivePet.Age}"/>
                                        <Span Text=" years · "/>
                                        <Span Text="{Binding MainPageVM.LatestWeight}"/>
                                        <Span Text=" kg"/>
                                    </FormattedString>
                                </Label.FormattedText>

                            </Label>

                            <!-- Health Tags -->
                            <FlexLayout Wrap="Wrap"
                                        Direction="Row"
                                        AlignItems="Start"
                                        JustifyContent="Start">

                                <!-- ? convert to bindable collection later -->
                                <Border BackgroundColor="#30FFFFFF"
                                        Padding="10,4"
                                        Margin="0,0,6,6"
                                        StrokeThickness="0">

                                    <Border.StrokeShape>
                                        <RoundRectangle CornerRadius="20"/>
                                    </Border.StrokeShape>

                                    <Label Text="Healthy (Placeholder)"
                                           FontSize="10"
                                           FontAttributes="Bold"
                                           TextColor="White"/>
                                </Border>

                                <Border BackgroundColor="#40F0C060"
                                        Padding="10,4"
                                        Margin="0,0,6,6"
                                        StrokeThickness="0">

                                    <Border.StrokeShape>
                                        <RoundRectangle CornerRadius="20"/>
                                    </Border.StrokeShape>

                                    <!-- ? medication reminder binding -->
                                    <Label Text="Meds 20:00(Placeholder)"
                                           FontSize="10"
                                           FontAttributes="Bold"
                                           TextColor="#7A4A00"/>
                                </Border>

                            </FlexLayout>

                        </VerticalStackLayout>

                    </Grid>

                </Border>

                <!-- QUICK STATS -->
                <Grid ColumnDefinitions="*,*"
                      ColumnSpacing="14">

                    <!-- Latest Weight -->
                    <Border Padding="16"
                            BackgroundColor="#40FFFFFF"
                            Stroke="#50FFFFFF"
                            StrokeThickness="1">

                        <Border.StrokeShape>
                            <RoundRectangle CornerRadius="20"/>
                        </Border.StrokeShape>

                        <VerticalStackLayout Spacing="6">

                            <Label Text="WEIGHT"
                                   FontSize="10"
                                   FontAttributes="Bold"
                                   TextColor="#5A7990"/>

                            <Label Text="{Binding MainPageVM.LatestWeight, StringFormat='{0} kg'}"
                                   FontSize="22"
                                   FontAttributes="Bold"
                                   TextColor="#1C3D52"/>

                            <!-- ? trend binding -->
                            <Label Text="Stable this week (Placeholder)"
                                   FontSize="11"
                                   TextColor="#6D8291"/>

                        </VerticalStackLayout>

                    </Border>

                    <!-- Mood -->
                    <Border Grid.Column="1"
                            Padding="16"
                            BackgroundColor="#40FFFFFF"
                            Stroke="#50FFFFFF"
                            StrokeThickness="1">

                        <Border.StrokeShape>
                            <RoundRectangle CornerRadius="20"/>
                        </Border.StrokeShape>

                        <VerticalStackLayout Spacing="6">

                            <Label Text="MOOD"
                                   FontSize="10"
                                   FontAttributes="Bold"
                                   TextColor="#5A7990"/>

                            <!-- ? latest mood binding -->
                            <Label Text="Happy 😊(Placeholder)"
                                   FontSize="22"
                                   FontAttributes="Bold"
                                   TextColor="#1C3D52"/>

                            <!-- ? date binding -->
                            <Label Text="Logged today"
                                   FontSize="11"
                                   TextColor="#6D8291"/>

                        </VerticalStackLayout>

                    </Border>

                </Grid>

                <!-- WEIGHT CHART -->
                <view:WeightChartView/>

                <!-- MOOD TIMELINE -->
                <VerticalStackLayout Spacing="10"
                                     IsVisible="{Binding MainPageVM.MoodTimeline.HasMoodData}">

                    <Label Text="MOOD HISTORY"
                           FontSize="11"
                           FontAttributes="Bold"
                           TextColor="#5A7990"/>

                    <Border BackgroundColor="#40FFFFFF"
                            Stroke="#50FFFFFF"
                            StrokeThickness="1"
                            Padding="12,8"
                            StrokeShape="RoundRectangle 16">

                        <ScrollView Orientation="Horizontal"
                                    HorizontalScrollBarVisibility="Never">

                            <HorizontalStackLayout Spacing="2"
                                                   BindableLayout.ItemsSource="{Binding MainPageVM.MoodTimeline.TimelineDays}">

                                <BindableLayout.ItemTemplate>
                                    <DataTemplate>
                                        <Border WidthRequest="16"
                                                HeightRequest="16"
                                                BackgroundColor="{Binding MoodColor}"
                                                StrokeThickness="0"
                                                Margin="2,2,2,2"
                                                StrokeShape="RoundRectangle 3">

                                            <Border.GestureRecognizers>
                                                <TapGestureRecognizer Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.MainPageVM.NavigateToMoodDateCommand}"
                                                                      CommandParameter="{Binding Date}"/>
                                            </Border.GestureRecognizers>

                                        </Border>
                                    </DataTemplate>
                                </BindableLayout.ItemTemplate>

                            </HorizontalStackLayout>

                        </ScrollView>

                    </Border>

                </VerticalStackLayout>

                <!-- MOOD TIMELINE EMPTY STATE -->
                <VerticalStackLayout Spacing="8"
                                     IsVisible="{Binding MainPageVM.MoodTimeline.HasMoodData, Converter={StaticResource InvertedBoolConverter}}"
                                     Padding="16"
                                     VerticalOptions="Start">

                    <Label Text="😊"
                           FontSize="32"
                           HorizontalTextAlignment="Center"/>

                    <Label Text="Start tracking your pet's mood in the calendar"
                           FontSize="12"
                           TextColor="#6D8291"
                           HorizontalTextAlignment="Center"/>

                </VerticalStackLayout>

                <!-- RECENT ACTIVITY -->
                <VerticalStackLayout Spacing="10">

                    <Label Text="RECENT ACTIVITY"
                           FontSize="11"
                           FontAttributes="Bold"
                           TextColor="#5A7990"/>

                    <!-- This should eventually become a CollectionView -->
                    <!-- ? recent activity collection -->

                    <!-- Activity Card -->
                    <Border Padding="14"
                            BackgroundColor="#35FFFFFF"
                            Stroke="#45FFFFFF"
                            StrokeThickness="1">

                        <Border.StrokeShape>
                            <RoundRectangle CornerRadius="18"/>
                        </Border.StrokeShape>

                        <Grid ColumnDefinitions="Auto,*,Auto"
                              ColumnSpacing="12">

                            <Border WidthRequest="42"
                                    HeightRequest="42"
                                    BackgroundColor="#30C4A8E0"
                                    StrokeThickness="0">

                                <Border.StrokeShape>
                                    <RoundRectangle CornerRadius="12"/>
                                </Border.StrokeShape>

                                <Label Text="💊"
                                       FontSize="18"
                                       HorizontalOptions="Center"
                                       VerticalOptions="Center"/>
                            </Border>

                            <VerticalStackLayout Grid.Column="1"
                                                 Spacing="2">

                                <!-- ? medication binding -->
                                <Label Text="Medication given"
                                       FontSize="14"
                                       FontAttributes="Bold"
                                       TextColor="#1C3D52"/>

                                <!-- ? details binding -->
                                <Label Text="Methimazole · with food(Placeholder)"
                                       FontSize="11"
                                       TextColor="#6D8291"/>

                            </VerticalStackLayout>

                            <!-- ? time binding -->
                            <Label Grid.Column="2"
                                   Text="08:00"
                                   FontSize="10"
                                   TextColor="#7B91A1"
                                   VerticalOptions="Start"/>

                        </Grid>

                    </Border>

                </VerticalStackLayout>



            </VerticalStackLayout>

        </ScrollView>

        <!-- BOTTOM NAVIGATION -->
        <Border Grid.Row="1"
                Margin="0"
                Padding="12,10,12,20"
                BackgroundColor="#E6C4A8E0"
                StrokeThickness="0"
                VerticalOptions="End">

            <Border.StrokeShape>
                <RoundRectangle CornerRadius="24,24,0,0"/>
            </Border.StrokeShape>

            <Border.Shadow>
                <Shadow Brush="#30000000"
                        Offset="0,-2"
                        Radius="10"
                        Opacity="0.18"/>
            </Border.Shadow>

            <Grid ColumnDefinitions="*,*,*">

                <!-- MAIN -->
                <Grid>

                    <VerticalStackLayout HorizontalOptions="Center"
                                         Spacing="2">

                        <Label Text="🏠"
                               FontSize="20"
                               HorizontalTextAlignment="Center"/>

                        <Label Text="Main"
                               FontSize="11"
                               FontAttributes="Bold"
                               TextColor="#4A1580"
                               HorizontalTextAlignment="Center"/>
                    </VerticalStackLayout>
                </Grid>

                <!-- CALENDAR -->
                <Grid Grid.Column="1">
                    <Grid.GestureRecognizers>
                        <TapGestureRecognizer Tapped="OnCalendarClicked"/>
                    </Grid.GestureRecognizers>

                    <VerticalStackLayout HorizontalOptions="Center"
                                         Spacing="2">

                        <Label Text="📅"
                               FontSize="20"
                               HorizontalTextAlignment="Center"/>

                        <Label Text="Calendar"
                               FontSize="11"
                               FontAttributes="Bold"
                               TextColor="#7A5D99"
                               HorizontalTextAlignment="Center"/>
                    </VerticalStackLayout>
                </Grid>

                <!-- PETS -->
                <Grid Grid.Column="2">
                    <Grid.GestureRecognizers>
                        <TapGestureRecognizer Tapped="OnPetsClicked"/>
                    </Grid.GestureRecognizers>

                    <VerticalStackLayout HorizontalOptions="Center"
                                         Spacing="2">

                        <Label Text="🐾"
                               FontSize="20"
                               HorizontalTextAlignment="Center"/>

                        <Label Text="Pets"
                               FontSize="11"
                               FontAttributes="Bold"
                               TextColor="#7A5D99"
                               HorizontalTextAlignment="Center"/>
                    </VerticalStackLayout>
                </Grid>

            </Grid>
        </Border>

    </Grid>

</ContentPage>