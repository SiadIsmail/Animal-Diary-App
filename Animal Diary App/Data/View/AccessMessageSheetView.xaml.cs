namespace Animal_Diary_App.Data.View;

/// <summary>The reusable grant-message sheet (grant ending / grant ended). Purely
/// VM-driven; the host page binds it to the shared AccessMessageViewModel.</summary>
public partial class AccessMessageSheetView : ContentView
{
    public AccessMessageSheetView() => InitializeComponent();
}
