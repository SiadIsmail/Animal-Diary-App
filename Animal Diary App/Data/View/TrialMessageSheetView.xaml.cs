namespace Animal_Diary_App.Data.View;

/// <summary>The reusable trial-message sheet (explainer / nudge / read-only). Purely
/// VM-driven; the host page binds it to the shared TrialMessageViewModel.</summary>
public partial class TrialMessageSheetView : ContentView
{
    public TrialMessageSheetView() => InitializeComponent();
}
