<?xml version="1.0" encoding="utf-8" ?>
<!-- AddAnythingSheetView — the "+" sheet listing this pet's log types. BindingContext
     is the JournalLogViewModel; picking a row raises RequestOpenSheet, which the page
     turns into the matching sheet sliding up in its place. -->
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:controls="clr-namespace:Animal_Diary_App.Data.View.Controls"
             xmlns:loc="clr-namespace:Animal_Diary_App.Helpers"
             xmlns:vms="clr-namespace:Animal_Diary_App.Data.ViewModels"
             x:DataType="vms:JournalLogViewModel"
             x:Class="Animal_Diary_App.Data.View.AddAnythingSheetView"
             x:Name="Root"
             InputTransparent="{Binding IsAddSheetVisible, Converter={StaticResource InvertedBoolConverter}}">

    <controls:FelovaBottomSheet IsPresented="{Binding IsAddSheetVisible}"
                                Title="{Binding AddSheetTitle}"
                                Subtitle="{Binding AddSheetSubtitle}"
                                DismissCommand="{Binding CloseAddSheetCommand}">
        <controls:FelovaBottomSheet.SheetContent>
            <!-- The body owns its scroller, as FelovaBottomSheet requires: this list is
                 unbounded — every custom tracker the owner defines adds a row — so past
                 a certain count it outgrows the sheet's 90%-of-screen cap, and the cap
                 only clips. Sized by MaximumHeightRequest from the code-behind rather
                 than left to fill: VerticalOptions="Start" plus a ceiling makes the sheet
                 hug three rows and scroll at fifteen. A plain filling ScrollView is the
                 regression FelovaBottomSheet.xaml warns about — on Android it measures to
                 FILL in this star row and stretches even a short sheet to full height.

                 BindableLayout, not CollectionView: CollectionView's RecyclerView wouldn't
                 render inside this auto-sized sheet until scrolled, and would nest a second
                 scroller inside this one. Safe because FelovaBottomSheet keeps its body
                 realised while hidden (translated off-screen, never IsVisible=false). -->
            <ScrollView x:Name="Body"
                        VerticalOptions="Start"
                        VerticalScrollBarVisibility="Never">
            <VerticalStackLayout Spacing="9" Padding="0,2,0,2">

                <!-- In this pet's care plan. -->
                <VerticalStackLayout Spacing="9"
                                     BindableLayout.ItemsSource="{Binding AddOptions}">
                    <BindableLayout.ItemTemplate>
                        <DataTemplate x:DataType="vms:AddOption">
                            <Border BackgroundColor="{StaticResource Glass}"
                                    Stroke="{StaticResource GlassLine}"
                                    StrokeThickness="1"
                                    StrokeShape="RoundRectangle 16,13,15,14"
                                    Padding="13,13">
                                <Border.GestureRecognizers>
                                    <TapGestureRecognizer
                                        Command="{Binding Source={x:Reference Root}, Path=BindingContext.SelectAddOptionCommand}"
                                        CommandParameter="{Binding .}"/>
                                </Border.GestureRecognizers>
                                <Grid ColumnDefinitions="36,*" ColumnSpacing="13">
                                    <Border Grid.Column="0" WidthRequest="36" HeightRequest="36"
                                            BackgroundColor="{StaticResource TealTint}"
                                            StrokeThickness="0" StrokeShape="RoundRectangle 12"
                                            VerticalOptions="Center">
                                        <Label Text="{Binding Icon}" FontSize="18"
                                               HorizontalOptions="Center" VerticalOptions="Center"/>
                                    </Border>
                                    <Label Grid.Column="1" Text="{Binding Label}"
                                           FontFamily="{StaticResource FontSans}" FontSize="14" FontAttributes="Bold"
                                           TextColor="{StaticResource Ink}" VerticalOptions="Center"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </BindableLayout.ItemTemplate>
                </VerticalStackLayout>

                <!-- Everything else the app can record. Same rows, quieter tint, so the
                     plan still reads as the pet's own routine. Hidden entirely once a
                     plan covers everything. -->
                <VerticalStackLayout Spacing="6" Margin="0,6,0,0"
                                     IsVisible="{Binding HasMoreOptions}">
                    <Label Text="{loc:Translate Journal_MoreToLog}"
                           FontFamily="{StaticResource FontSans}"
                           FontSize="11"
                           FontAttributes="Bold"
                           CharacterSpacing="1.2"
                           TextColor="{StaticResource InkTertiary}"/>
                    <!-- Says where the permanent version of this lives, so the one-off
                         log doesn't have to double as a commitment. -->
                    <Label Text="{loc:Translate Journal_MoreToLogHint}"
                           FontFamily="{StaticResource FontSans}"
                           FontSize="12"
                           LineHeight="1.35"
                           TextColor="{StaticResource InkSecondary}"
                           Margin="0,0,0,3"/>
                </VerticalStackLayout>

                <VerticalStackLayout Spacing="9"
                                     BindableLayout.ItemsSource="{Binding MoreOptions}">
                    <BindableLayout.ItemTemplate>
                        <DataTemplate x:DataType="vms:AddOption">
                            <Border BackgroundColor="{StaticResource GlassSoft}"
                                    Stroke="{StaticResource GlassLine}"
                                    StrokeThickness="1"
                                    StrokeShape="RoundRectangle 16,13,15,14"
                                    Padding="13,13">
                                <Border.GestureRecognizers>
                                    <TapGestureRecognizer
                                        Command="{Binding Source={x:Reference Root}, Path=BindingContext.SelectAddOptionCommand}"
                                        CommandParameter="{Binding .}"/>
                                </Border.GestureRecognizers>
                                <Grid ColumnDefinitions="36,*" ColumnSpacing="13">
                                    <Border Grid.Column="0" WidthRequest="36" HeightRequest="36"
                                            BackgroundColor="{StaticResource Well}"
                                            StrokeThickness="0" StrokeShape="RoundRectangle 12"
                                            VerticalOptions="Center">
                                        <Label Text="{Binding Icon}" FontSize="18"
                                               HorizontalOptions="Center" VerticalOptions="Center"/>
                                    </Border>
                                    <Label Grid.Column="1" Text="{Binding Label}"
                                           FontFamily="{StaticResource FontSans}" FontSize="14" FontAttributes="Bold"
                                           TextColor="{StaticResource InkSecondary}" VerticalOptions="Center"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </BindableLayout.ItemTemplate>
                </VerticalStackLayout>

                <!-- ── Not a log type ───────────────────────────────────────
                     A question for the vet records nothing about the animal, so it
                     sits apart from both lists above. Dropping it into "everything
                     else the app can record" would make it read as a tracker, which
                     is the one thing it must never become — it never reaches the
                     timeline, the chips or the care plan (Data/Models/VetQuestion.cs).
                     Its own heading names the occasion rather than the person. -->
                <VerticalStackLayout Spacing="6" Margin="0,10,0,0">
                    <Label Text="{Binding VetQuestionHeading}"
                           FontFamily="{StaticResource FontSans}"
                           FontSize="11"
                           FontAttributes="Bold"
                           CharacterSpacing="1.2"
                           TextColor="{StaticResource InkTertiary}"/>

                    <Border BackgroundColor="{StaticResource GlassSoft}"
                            Stroke="{StaticResource GlassLine}"
                            StrokeThickness="1"
                            StrokeShape="RoundRectangle 16,13,15,14"
                            Padding="13,13">
                        <Border.GestureRecognizers>
                            <TapGestureRecognizer
                                Command="{Binding Source={x:Reference Root}, Path=BindingContext.SelectAddOptionCommand}"
                                CommandParameter="{Binding VetQuestionOption}"/>
                        </Border.GestureRecognizers>
                        <Grid ColumnDefinitions="36,*" ColumnSpacing="13">
                            <Border Grid.Column="0" WidthRequest="36" HeightRequest="36"
                                    BackgroundColor="{StaticResource Well}"
                                    StrokeThickness="0" StrokeShape="RoundRectangle 12"
                                    VerticalOptions="Center">
                                <Label Text="{Binding VetQuestionOption.Icon}" FontSize="18"
                                       HorizontalOptions="Center" VerticalOptions="Center"/>
                            </Border>
                            <Label Grid.Column="1" Text="{Binding VetQuestionLabel}"
                                   FontFamily="{StaticResource FontSans}" FontSize="14" FontAttributes="Bold"
                                   TextColor="{StaticResource InkSecondary}" VerticalOptions="Center"/>
                        </Grid>
                    </Border>
                </VerticalStackLayout>

            </VerticalStackLayout>
            </ScrollView>
        </controls:FelovaBottomSheet.SheetContent>
    </controls:FelovaBottomSheet>
</ContentView>
