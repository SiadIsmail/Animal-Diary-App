<?xml version="1.0" encoding="UTF-8" ?>
<ResourceDictionary 
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml">

    <!-- Note: For Android please see also Platforms\Android\Resources\values\colors.xml -->

    <Color x:Key="Primary">#512BD4</Color>
    <Color x:Key="PrimaryDark">#ac99ea</Color>
    <Color x:Key="PrimaryDarkText">#242424</Color>
    <Color x:Key="Secondary">#DFD8F7</Color>
    <Color x:Key="SecondaryDarkText">#9880e5</Color>
    <Color x:Key="Tertiary">#2B0B98</Color>

    <Color x:Key="White">White</Color>
    <Color x:Key="Black">Black</Color>
    <Color x:Key="Magenta">#D600AA</Color>
    <Color x:Key="MidnightBlue">#190649</Color>
    <Color x:Key="OffBlack">#1f1f1f</Color>

    <Color x:Key="Gray100">#E1E1E1</Color>
    <Color x:Key="Gray200">#C8C8C8</Color>
    <Color x:Key="Gray300">#ACACAC</Color>
    <Color x:Key="Gray400">#919191</Color>
    <Color x:Key="Gray500">#6E6E6E</Color>
    <Color x:Key="Gray600">#404040</Color>
    <Color x:Key="Gray900">#212121</Color>
    <Color x:Key="Gray950">#141414</Color>

    <SolidColorBrush x:Key="PrimaryBrush" Color="{StaticResource Primary}"/>
    <SolidColorBrush x:Key="SecondaryBrush" Color="{StaticResource Secondary}"/>
    <SolidColorBrush x:Key="TertiaryBrush" Color="{StaticResource Tertiary}"/>
    <SolidColorBrush x:Key="WhiteBrush" Color="{StaticResource White}"/>
    <SolidColorBrush x:Key="BlackBrush" Color="{StaticResource Black}"/>

    <SolidColorBrush x:Key="Gray100Brush" Color="{StaticResource Gray100}"/>
    <SolidColorBrush x:Key="Gray200Brush" Color="{StaticResource Gray200}"/>
    <SolidColorBrush x:Key="Gray300Brush" Color="{StaticResource Gray300}"/>
    <SolidColorBrush x:Key="Gray400Brush" Color="{StaticResource Gray400}"/>
    <SolidColorBrush x:Key="Gray500Brush" Color="{StaticResource Gray500}"/>
    <SolidColorBrush x:Key="Gray600Brush" Color="{StaticResource Gray600}"/>
    <SolidColorBrush x:Key="Gray900Brush" Color="{StaticResource Gray900}"/>
    <SolidColorBrush x:Key="Gray950Brush" Color="{StaticResource Gray950}"/>

    <!-- ============================================================= -->
    <!-- Felova redesign tokens (see Designs/felova-redesign.html)      -->
    <!-- ============================================================= -->

    <!-- Surfaces -->
    <Color x:Key="Paper">#D9EFE9</Color>       <!-- rockpool page ground         -->
    <Color x:Key="Card">#99FFFFFF</Color>      <!-- primary card surface (60% white) -->
    <Color x:Key="Line">#B3FFFFFF</Color>      <!-- hairline borders (white stroke) -->

    <!-- Ink / text -->
    <Color x:Key="Ink">#0D3A3C</Color>         <!-- primary text — blue-green ink -->
    <Color x:Key="InkSecondary">#3A6A6B</Color><!-- secondary text               -->
    <Color x:Key="InkTertiary">#7CA2A1</Color> <!-- tertiary / metadata          -->

    <!-- Rockpool — brand accent (teal/blue-green) -->

    <!-- Honey — medication / attention -->
    <Color x:Key="Honey">#D99A3E</Color>
    <Color x:Key="HoneyDeep">#8A5D14</Color>

    <!-- Sage — stable / given / good -->

    <!-- Clay — alerts, used sparingly -->
    <Color x:Key="Clay">#C05F5F</Color>
    <Color x:Key="ClayTint">#F7E5E2</Color>


    <!-- Brushes for the common surface / accent tokens -->
    <SolidColorBrush x:Key="PaperBrush" Color="{StaticResource Paper}"/>
    <SolidColorBrush x:Key="CardBrush" Color="{StaticResource Card}"/>
    <SolidColorBrush x:Key="LineBrush" Color="{StaticResource Line}"/>
    <SolidColorBrush x:Key="InkBrush" Color="{StaticResource Ink}"/>
    <SolidColorBrush x:Key="HoneyBrush" Color="{StaticResource Honey}"/>
    <SolidColorBrush x:Key="ClayBrush" Color="{StaticResource Clay}"/>

    <!-- =================================================================== -->
    <!-- ROCKPOOL · WARM  (Journal reskin — Designs/felova-journal-rockpool-warm.html) -->
    <!-- Additive tokens: do NOT reuse the older redesign keys above so the   -->
    <!-- MainPage / PetsPage / MedicationsPage visuals stay unchanged.        -->
    <!-- Alpha hexes are #AARRGGBB (e.g. 13% => 0x21).                        -->
    <!-- =================================================================== -->

    <!-- Sea gradient stops + warm sand -->
    <Color x:Key="SeaHi">#E8F7F1</Color>       <!-- mint highlight            -->
    <Color x:Key="SeaMid">#C4E6E3</Color>      <!-- aqua mid                  -->
    <Color x:Key="SeaBlue">#B7DCEC</Color>     <!-- sea blue                  -->
    <Color x:Key="Sand">#EDDFC0</Color>        <!-- warm sand (bottom glow)   -->
    <Color x:Key="SeaGradTop">#DDF2EC</Color>  <!-- linear top                -->

    <!-- Ink reuses the shared Ink / InkSecondary / InkTertiary (#0D3A3C /
         #3A6A6B / #7CA2A1) — identical values, no new keys needed. -->

    <!-- Teal accent (translucent, glass-friendly) -->
    <Color x:Key="Teal">#149081</Color>
    <Color x:Key="TealDeep">#0C6A5D</Color>
    <Color x:Key="TealTint">#21149081</Color>   <!-- 13% -->
    <Color x:Key="TealLine">#47149081</Color>   <!-- 28% -->

    <!-- Blue — weigh-in accent -->
    <Color x:Key="Blue">#3E8FB0</Color>
    <Color x:Key="BlueTint">#243E8FB0</Color>   <!-- 14% -->
    <Color x:Key="BlueInk">#2A6E8C</Color>      <!-- weigh-in icon fg -->

    <!-- Rose — glucose accent (the design system's rose / rose-tint / rose-deep;
         glucose is new, so these join the warm palette now). -->
    <Color x:Key="Rose">#BE5F76</Color>
    <Color x:Key="RoseTint">#24BE5F76</Color>   <!-- 14% -->
    <Color x:Key="RoseDeep">#8C3B50</Color>

    <!-- Honey — medication (warm variant, distinct from shared Honey) -->
    <Color x:Key="HoneyWarm">#D9973C</Color>
    <Color x:Key="HoneyWarmTint">#29D9973C</Color> <!-- 16% -->
    <Color x:Key="HoneyWarmDeep">#8A5A12</Color>

    <!-- Violet — seizure accent (timeline icon tile) -->
    <Color x:Key="Violet">#584A8A</Color>
    <Color x:Key="VioletTint">#26584A8A</Color> <!-- 15% -->
    <Color x:Key="VioletSoftTint">#267B6BAE</Color> <!-- 15% of a lighter violet;
         the Manage page's care-plan rows use this rather than VioletTint -->


    <!-- Warm paper (mood note card) + washi tape -->
    <Color x:Key="PaperWarm">#FFF9EC</Color>
    <Color x:Key="NoteInk">#4A4433</Color>      <!-- serif-italic note text  -->
    <Color x:Key="Washi">#A6BEE2D8</Color>      <!-- 65% mint tape           -->

    <!-- Glass surfaces (frosted whites) -->
    <Color x:Key="Glass">#99FFFFFF</Color>      <!-- 60% -->
    <Color x:Key="GlassSoft">#6BFFFFFF</Color>  <!-- 42% -->
    <Color x:Key="GlassLine">#D9FFFFFF</Color>  <!-- 85% -->
    <Color x:Key="Well">#57FFFFFF</Color>       <!-- 34% recessed well -->

    <!-- ══════════════ NIGHT (the Constellation, and only the Constellation) ══════════════
         The one dark surface in the app. It is NOT a dark theme and must not become the
         seed of one: these are the rockpool tokens after sundown, mixed to sit under the
         same ink (#0D3A3C is the daylight text colour and the night ground's own hue).

         The Star* set is the daylight tracker accents lifted to read on that ground — a
         weigh-in is blue in both places, a seizure violet in both. They are LIGHTER than
         their daytime siblings for contrast, never a different hue, so nothing changes
         identity between the Journal and the sky. None of them encodes a value; a colour
         here says what kind of thing was recorded, exactly as the symbol does. -->
    <!-- The ground is the rockpool wash after dark: the same top→mid→bottom vertical
         gradient WaterBackground paints in daylight, walked down in value and kept
         green-teal rather than navy. A blue-black sky read as a different app. -->
    <Color x:Key="NightTop">#123C3E</Color>     <!-- sky ground, top    -->
    <Color x:Key="NightMid">#0B2A31</Color>     <!-- sky ground, middle -->
    <Color x:Key="NightBottom">#08191F</Color>  <!-- sky ground, bottom -->

    <!-- The two ambient glows WaterBackground already uses, at night strength: a mint
         one up top and the warm SAND one rising off the bottom edge. The sand is the
         signature — it is what makes this the WARM rockpool rather than a cold one. -->
    <Color x:Key="NightGlowMint">#8FE3D2</Color>
    <Color x:Key="NightGlowSand">#EDDFC0</Color>

    <Color x:Key="StarDust">#C4E6E3</Color>     <!-- distant, data-free specks -->
    <!-- The pet's own figure. A shade brighter than the dust so it reads as drawn
         rather than scattered, and still well below every event symbol — it must never
         compete with something that actually happened. -->
    <Color x:Key="NightAsterism">#D8EFE6</Color>
    <Color x:Key="SkyPath">#7FD4C4</Color>      <!-- the timeline's thin glow  -->

    <!-- The five hues one pet's atmosphere may lean towards (SkySignature.Accents) —
         the app's own five accents, one step further muted for a night ground.

         DELIBERATELY NOT the Star* set below. Those say which KIND of thing was
         recorded; if a whole sky could glow in one of them, an owner whose ambient hue
         happened to be rose would have a rose wash behind their rose glucose readings
         and the legend would stop meaning anything. These are greyer and dimmer than
         any category can be, so they only ever read as weather. -->
    <Color x:Key="SkyAccentTeal">#6FBFB2</Color>
    <Color x:Key="SkyAccentBlue">#6FA8C6</Color>
    <Color x:Key="SkyAccentHoney">#C9A76B</Color>
    <Color x:Key="SkyAccentRose">#C08494</Color>
    <Color x:Key="SkyAccentViolet">#8E86BE</Color>

    <!-- A scrap of night, tile-sized: the legend's symbols sit on these out on the
         daylight page, so a star drawn for a dark ground still reads. Same shape and
         size as the Journal timeline's tinted icon tiles. -->
    <Color x:Key="NightTile">#14343B</Color>

    <!-- The text that sits ON the night: the empty-state line, the dates under the sky,
         the fold dial's label, and the peek that opens when a star is tapped. Ink for a
         heading, SkyInk for everything secondary.

         Everything else on the page — the page heading, the range tabs, the legend, the
         hints — is ordinary daylight Ink, because the page is a daylight page. The
         all-dark version's chrome tokens (SkyGlass/SkyWell/SkyLine) stayed deleted with
         it; don't reintroduce those without reintroducing the mistake. -->
    <Color x:Key="SkyText">#EAF6F2</Color>
    <Color x:Key="SkyInk">#9FC4C0</Color>
    <!-- Hairline for the one control that sits ON the night: the fold dial. -->
    <Color x:Key="SkyLineSoft">#33FFFFFF</Color>

    <Color x:Key="StarMood">#5FD3C0</Color>       <!-- teal   -->
    <Color x:Key="StarWeight">#7FC4E4</Color>     <!-- blue   -->
    <Color x:Key="StarGlucose">#EC8FA5</Color>    <!-- rose   -->
    <Color x:Key="StarAppetite">#F0BE72</Color>   <!-- honey  -->
    <Color x:Key="StarWater">#8FD8F0</Color>      <!-- water blue, lighter than weight -->
    <Color x:Key="StarSeizure">#AC9DE8</Color>    <!-- violet -->
    <Color x:Key="StarMedication">#FFE1A8</Color> <!-- warm cream — doses are the app's own ask -->
    <Color x:Key="StarCustom">#CFE9DF</Color>     <!-- pale mint — the owner's own trackers -->

    <!-- Mood scale (warm variant — matches HTML m-great..m-unwell) -->
    <Color x:Key="MGreat">#FF2BBC89</Color>
    <Color x:Key="MGood">#FF89E166</Color>
    <Color x:Key="MOkay">#FFE2F250</Color>
    <Color x:Key="MLow">#FFF29C33</Color>
    <Color x:Key="MUnwell">#FFDC4141</Color>

    <!-- Brushes for surfaces / strokes used by the Journal -->
    <SolidColorBrush x:Key="TealBrush" Color="{StaticResource Teal}"/>
    <SolidColorBrush x:Key="TealDeepBrush" Color="{StaticResource TealDeep}"/>
    <SolidColorBrush x:Key="TealTintBrush" Color="{StaticResource TealTint}"/>
    <SolidColorBrush x:Key="TealLineBrush" Color="{StaticResource TealLine}"/>
    <SolidColorBrush x:Key="BlueBrush" Color="{StaticResource Blue}"/>
    <SolidColorBrush x:Key="BlueTintBrush" Color="{StaticResource BlueTint}"/>
    <SolidColorBrush x:Key="HoneyWarmBrush" Color="{StaticResource HoneyWarm}"/>
    <SolidColorBrush x:Key="HoneyWarmTintBrush" Color="{StaticResource HoneyWarmTint}"/>
    <SolidColorBrush x:Key="PaperWarmBrush" Color="{StaticResource PaperWarm}"/>
    <SolidColorBrush x:Key="WashiBrush" Color="{StaticResource Washi}"/>
    <SolidColorBrush x:Key="GlassBrush" Color="{StaticResource Glass}"/>
    <SolidColorBrush x:Key="GlassSoftBrush" Color="{StaticResource GlassSoft}"/>
    <SolidColorBrush x:Key="GlassLineBrush" Color="{StaticResource GlassLine}"/>
    <SolidColorBrush x:Key="WellBrush" Color="{StaticResource Well}"/>
</ResourceDictionary>